# Copie para .env e ajuste se necessário (não commite o .env no Git)
AUTH_USER=vinicius.ouriques
AUTH_PASSWORD=SuaSenhaAqui

# Opcional: hash pré-gerado (node scripts/gerar-hash-senha.js "SuaSenha")
# AUTH_PASSWORD_HASH=salt:hash
